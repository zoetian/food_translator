OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-5-mini
OPENAI_IMAGE_MODEL=gpt-image-1
# Max /api/identify calls per day that actually hit OpenAI (cached repeats don't count).
DAILY_REQUEST_CAP=10
CORS_ORIGINS=["http://localhost:5173"]
